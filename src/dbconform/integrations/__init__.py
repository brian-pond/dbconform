"""
dbconform integrations namespace.

Optional integration submodules live here (e.g. ``dbconform.integrations.dbt``).
Each submodule is conditionally importable: importing without the required extra
raises ``ImportError`` with an actionable install message.

See docs/requirements/01-functional.md (dbt integration, BR-DBT-001).
"""
